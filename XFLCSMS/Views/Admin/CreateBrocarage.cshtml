@model XFLCSMS.Models.Brocarage.Brokerage;

<!-- Main content -->
<section class="content">

    <h1>Create</h1>

    <h4>Brokerage</h4>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-controller="Home" asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="@Model.BrokerageHouseName" class="control-label"></label>
                    <input asp-for="@Model.BrokerageHouseName" class="form-control" />
                    <span asp-validation-for="@Model.BrokerageHouseName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.BrokerageHouseAcronym" class="control-label"></label>
                    <input asp-for="@Model.BrokerageHouseAcronym" class="form-control" />
                    <span asp-validation-for="@Model.BrokerageHouseAcronym" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div>
        <a asp-controller ="Admin" asp-action="BrocarageHouseList">Back to List</a>
    </div>

</section>
<!-- /.content -->

<script type="text/javascript">
    // Define a function named preventback
    function preventback() {
        // Forward the browser's history by one step, effectively preventing the user from going back
        window.history.forward();
    }

    // Set a timeout to call the preventback function immediately after the script is executed
    setTimeout("preventback()", 0);

    // When the window loads, assign a null function to the onload event to effectively do nothing
    window.onload = function () { null };
</script>
      